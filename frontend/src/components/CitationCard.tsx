import {safeSourceUrl,describeMode} from '@/services/protocol.mjs';
export default function CitationCard({citation,index=0}:{citation:any,index?:number}) {const url=safeSourceUrl(citation.source_url);return <article className="source-card"><div className="source-heading"><span className="source-index">{citation.citation_id||index+1}</span><div><strong>{citation.title||citation.source||'Retrieved source'}</strong><small>{citation.authority||citation.provider||describeMode(citation.mode||'local')}{citation.page?` · Page ${citation.page}`:''}</small></div></div><blockquote>{citation.excerpt||citation.text||'No excerpt supplied. Inspect the source before relying on this record.'}</blockquote>{url?<a href={url} target="_blank" rel="noreferrer">Open supplied source ↗</a>:<span className="source-locator">{citation.locator||citation.publication_number||citation.source||'Source URL unavailable'}</span>}</article>;}
