import { clampScore } from '@/services/protocol.mjs';
export default function ConfidenceScore({score,label='Evidence grounding',description}:{score:unknown,label?:string,description?:string}) {const value=clampScore(score);return <div className="score-card"><span>{label}</span><strong>{value===null?'—':value}<small>{value===null?'Not assessed':' / 100'}</small></strong><div className="meter-track" role="meter" aria-label={label} aria-valuemin={0} aria-valuemax={100} aria-valuenow={value??undefined}><i style={{width:`${value??0}%`}}/></div><p>{description || 'Screening quality; not legal correctness.'}</p></div>;}
