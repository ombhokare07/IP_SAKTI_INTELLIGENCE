export default function ComparisonTable({rows,jurisdictions}:{rows:any[],jurisdictions:string[]}) {if(!rows?.length)return <div className="empty-state"><strong>No comparable requirements loaded</strong><p>Load a source corpus or enable the clearly labelled offline fixtures to exercise comparison.</p></div>;return <div className="table-scroll"><table><caption>Requirements in the selected stored sources</caption><thead><tr><th scope="col">Requirement</th>{jurisdictions.map(j=><th scope="col" key={j}>{j}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.field}><th scope="row">{row.field.replaceAll('_',' ')}</th>{jurisdictions.map(j=><td key={j}>{row.jurisdictions[j]?.length?row.jurisdictions[j].map((cell:any,i:number)=><div className="requirement-cell" key={i}><strong>{cell.requirement.label}</strong><p>{cell.requirement.description}</p><small>{cell.version} · {cell.requirement.mandatory?'Required by supplied source':'Optional in supplied source'}</small></div>):<span className="muted">No evidence in this corpus</span>}</td>)}</tr>)}</tbody></table></div>;}
